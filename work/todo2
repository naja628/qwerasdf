* merge overlapping shapes (at least when making copies)

* rewrite transform hooks 
* fine transforms hook
* undo/redo mechanisim with nested autosaves (under v)
* polar grid
* set or unset weaveback

* pin commands?

* circle arcs?

Fixes:
changing `wy` instant feedback wanted
when loading what happens if we're (eg) mid-weave?
rm all weaves into a shape when changing div number
menu cut when changing start resolution

Tweaks:
not super convinced about scroll to turn around control when drawing weaves
