want (v2 another time :P) :
	hooks big refactor
	zen view
	pdf guide pages (with marks for nails)
	use V for pinning terminal commands
	autosaves (+ weird nested system? for rewind)
	more shapes: interpolated curves, arcs, ellipses?
	visual linear transforms?, register transforms to shapes etc
	maybe more view options? ruler, grid etc
	more termcommands : dump, point
	commandline options? 

+ "details":
	interactive_set_div, interactive_rotation
	color picker pre-render thing
	disambiguate thingy
	finer weave selection
	circle around problems?? 
		(ie when drawing weaves choose the number of turns made)
	swap circle ends (when drawing)

